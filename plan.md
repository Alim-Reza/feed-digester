# Slice status

Tracks progress through the implementation slices in `plan.md` §6. Updated at
the end of each slice.

> **⚠️ `plan.md` and `PLAN.md` are the same file on this Mac.** macOS's default
> filesystem (APFS) is case-insensitive, so a write to one is a write to the
> other. At some point this slice tracker's content overwrote the actual
> "approved architecture" document (`plan.md` §1–§9: schema, citation-
> validation rules, the batch-loop details, etc.) that CLAUDE.md and
> ARCHITECTURE.md still point to — discovered at the start of slice 4, with no
> git history to recover it from (the repo had zero commits at the time).
> Surviving detail lives scattered across `ARCHITECTURE.md`, `docs/adr/`,
> `spec.md`, `grill-section*.md`, and this file's own per-slice notes; slice 4
> was implemented from those. **Don't recreate a `plan.md` with real content
> until this collision is fixed** (e.g. rename this tracker to `SLICES.md`),
> or the next write will destroy it again.

| #   | Slice                        | Status         | Notes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| --- | ---------------------------- | -------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 0   | Scaffold                     | ✅ Done        | Next.js (App Router, TS strict) + pnpm via corepack, ESLint/Prettier, Vitest, pino (+pino-roll, pino-pretty), Zod config loader (`digest.config.ts` → `src/config/`), `.env.example`, MIT `LICENSE`, README, `ARCHITECTURE.md`, `CLAUDE.md`, ADRs 0001–0003. `pnpm lint && pnpm test && pnpm build` pass.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| 1   | DB & repositories            | ✅ Done        | Drizzle schema (13 tables, §4), WAL + foreign_keys client (`src/db/client.ts`), migration generator/runner (`drizzle-kit`, `src/db/migrate.ts`, `pnpm db:generate`/`db:migrate`), repositories for posts/runs/runEvents/runCommands/settings with dedup, status-machine, retry, and claim-next-command semantics. 24 repository tests on an in-memory migrated DB pass. Digest/cluster/job-opening repositories are added alongside the stages that need them (slices 8–9) rather than speculatively now.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| 2   | Runner & worker              | ✅ Done        | `src/pipeline/`: `Stage`/`StageContext` interfaces, `AwaitingUserSignal`, `runBatchLoop` (the select→process→commit-in-one-transaction loop from plan §3.3), `runPipeline` (resumes from `currentStage`, distinguishes `awaiting_user` from `failed`), stub stages carrying the full `posts.processingStatus` state machine end to end. `src/worker/`: `pollCommands` (claims one `run_commands` row per tick, one run at a time via `runs.hasActiveRun`), `checkSchedule` (daily-at-08:00 + catch-up), `main.ts` (resumes interrupted runs on startup, then polls every 2s / checks schedule every 60s). `pnpm dev`/`pnpm start` now run web+worker via `concurrently`. 17 new tests cover: crash-mid-stage → resume finishes without reprocessing (verified which post IDs were and weren't re-touched), awaiting_user vs. failed outcomes, one-run-at-a-time locking, and schedule/catch-up timing. Worker smoke-tested live (`pnpm dev:worker`): migrated the DB, opened WAL mode, wrote rotating logs.                                                                                                                                                                                                |
| 3   | Collector parsing            | ✅ Done        | **Major finding**: as of 2026-09-25, LinkedIn's feed no longer has any semantic class names at all (`feed-shared-*`/`update-components-*` are gone) — it renders through a server-driven UI system with fully hashed/atomic CSS classes (`_1274a681`, `cf8121e6`, ...) and no `data-urn` on post containers. The original selectors.ts (written from training-data knowledge) matched zero posts on a real capture. Confirmed via a new diagnostic tool, `pnpm diagnose:feed` (`scripts/diagnose-feed.ts`), which walks every frame and inlines shadow DOM (`src/collector/composedSerializer.ts`) since a plain HTML snapshot can miss shadow-rendered content. **Rebuilt around ARIA instead of CSS**: post containers are `[role="listitem"]` (filtered to ones with post text or a "Hide post by "/"Open control menu for post by " control, deduped to outermost so nested comment listitems aren't double-captured); author name comes from that control's aria-label; post text from `[data-testid="expandable-text-box"]`; headline/timestamp are positionally the first/next `<p>` siblings, excluding the connection-degree bullet, "X likes this" social-proof lines, and repost lines (a real bug caught and fixed: a "likes this" line was initially mis-picked as the headline). An activity id is only opportunistically recoverable from a nested comment's `componentkey` (`urn:li:comment:(activity:<id>,...)`) — there's no reliable container-level URN anymore, so most posts fall back to hash-based dedup rather than exact URN dedup, a real deviation from the original plan worth remembering. Only `<video>`-tag presence is confidently detected for `contentType`; poll/image/article/document all default to `'text'` rather than guess at unverified markup (an avatar `<img>` on every post makes naive image-detection false-positive on everything). `pnpm collect:fixtures` now uses this same detection. 7 real fixtures were extracted from an actual logged-in capture (`scripts/extract-fixtures-from-dump.ts`) alongside 33 synthetic ones covering shapes the one real session didn't happen to show (sponsored, reposts — still unverified against real markup). `tests/fixtures/linkedin/README.md` warns real fixtures must not be pushed to a public remote. 121 tests total, including regression tests pinned to the real captured posts. `pnpm lint && pnpm test && pnpm build` pass. |
| 4   | Collector live                | ✅ Done        | `src/collector/`: `pacing.ts` (randomized scroll step + pause, with an occasional longer "reading" pause — grill B2/Q5), `stopConditions.ts` (max duration / max posts / max consecutive already-seen, whichever first — grill C6), `detection.ts` (URL-based page-state classifier: feed / checkpoint / login_wall / unknown — best-effort like slice 3's selectors, unverified against a real checkpoint), `extractSnippet.ts` (in-page snippet: finds real posts the same way slice 3's fixture tool does, expands "see more" only for posts in the viewport before reading `outerHTML` — grill C3 — and flags likely visual-media posts for a screenshot), `driver.ts` (`CollectorDriver` interface + a real Playwright-backed implementation, mirroring the `Classifier`/`LLMProvider` swappable-interface pattern so the orchestration loop is testable against a fake), `scroller.ts` (`collectFeed`: the human-paced loop — dedup via `posts.insertOrTouch`, an HTML snapshot saved to `<dataDir>/snapshots/` on a parse failure, an element screenshot to `<dataDir>/images/` + a new `post_images` row for likely-visual posts, a checkpoint/login-wall pause that polls up to `checkpointWaitMinutes` before giving up), `loginFlow.ts` (the "Log in" `run_commands` flow: opens the active profile headed via `browser.ts`, waits for you to close the window — reachable right now via `pnpm login`, ahead of slice 11's UI). `src/pipeline/stages/collect.ts` wires this into a real `Stage`, opening `config.profiles.active` and translating an unresolved checkpoint into `AwaitingUserSignal` (so the run pauses to `awaiting_user`, per grill B3/Q8) — whatever was already collected stays committed, so a later `process` command finishes it without re-collecting. **Made `Stage.run`/`runPipeline` async** (a real browser is inherently promise-based) and updated `worker/poller.ts`/`worker/scheduler.ts`/`worker/main.ts` to await it, with busy-guards in `main.ts`'s interval callbacks since a poll or schedule check can now be mid-flight for minutes. Profile selection ("main"/"collector") is config-driven (`config.profiles.active`, already overridable via `settings`), not a `run_commands` payload field. New `post_images` repository (schema/migration already existed from slice 1). 29 new tests: pure pacing/stop-condition/URL-detection logic, `collectFeed` against a fake `CollectorDriver` (maxPosts/consecutive-seen/duration stops, screenshot capture, parse-failure snapshot, checkpoint wait-and-resume, checkpoint timeout), `runCollectStage` wiring (always closes the session, even on error), and updated worker dispatch tests (`collect`/`login` no longer touch a real browser via injectable `openSession`/`loginFlow`/`stagesForCommand` overrides). Collector tests still use only saved fixtures/fakes — no automated test opens a real browser (grill J9); the live path needs the one pending manual step below. **Also found and flagged** (see the warning above): `plan.md`/`PLAN.md` collide on this case-insensitive filesystem, and the full architecture doc was lost as a result — not fixed in this slice, just documented. `pnpm lint && pnpm test && pnpm build` pass. |
| 5   | OCR + filter + language       | ✅ Done        | **Schema gap found and fixed**: slice 4's `collect` stage never persisted `parsePost`'s already-detected `isSponsored`/`isConnectionSuggestion`/`isPoll` flags — no columns existed for them. Added all three to `posts` (migration `0001_narrow_drax.sql`), plumbed through `posts.insertOrTouch` and `collector/scroller.ts`, so the filter stage below actually has signal to act on. `src/services/ocr/`: `OcrProvider` interface (same swappable-interface pattern as `Classifier`/`LLMProvider`/`CollectorDriver`) + a real `tesseract.js`-backed implementation (`eng`+`ben`, grill C5) not unit tested directly (loads real trained-data models); `textCheck.ts`'s `looksLikeRealText` is the "does this make sense" check from grill C5 — counts `\p{L}` **and** `\p{M}` (combining marks), since Bangla vowel signs/virama are marks, not letters, and counting letters alone undercounted real Bangla OCR text as noise (caught by a failing test against real Bangla). `src/services/language/detect.ts`: language detection via `franc`, but using `francAll`'s full ranked candidate list with a relative-score threshold (0.8 of the top score) rather than trusting `franc()`'s single top guess — found empirically that short/ambiguous real English text (e.g. "A substantive post about distributed systems...") gets misranked #1 as French by trigram frequency alone, with English a close #2; checking the full ranked list catches that without losing the ability to actually detect and drop genuinely different-language text (verified against real French, which doesn't come close in the ranking). Undetermined-length text defaults to `'en'` (grill C12 is about real foreign content, not short-text uncertainty). `src/pipeline/stages/ocr.ts`: `new → ocr_done`, OCR only on kept `post_images`, discards (`kept=false` + deletes the file) any image whose text doesn't look real, combines kept images' text onto `posts.ocrText`; the tesseract worker is created lazily (only once a batch actually needs it) and disposed once at the end — most posts have no images and never touch it. Recognize calls run **outside** `db.transaction()` and are committed per batch afterward — better-sqlite3 transactions must be synchronous, so `runBatchLoop` (which wraps `processBatch` directly in a transaction) doesn't fit async I/O; this is a real constraint worth remembering for any future async stage. `src/pipeline/stages/filter.ts`: cheap deterministic rules (spec's "Cheap deterministic filtering" step) — allowlist bypasses everything else (per the existing `config/schema.ts` comment), then blocklist/sponsored/connection-suggestion/poll/celebration-phrase/low-text-video/disallowed-language, first match wins, `dropReason` recorded; detected language is persisted on the post either way. Uses the existing synchronous `runBatchLoop` unchanged — filtering needs no async I/O. New deps: `tesseract.js`, `franc` (pure ESM, no network at runtime beyond tesseract's one-time trained-data download). `tesseract.js`'s postinstall script needed approval on this pnpm version — added `allowBuilds: tesseract.js: true` to `pnpm-workspace.yaml` after inspecting it (just an opencollective donation message) so a clean install doesn't hang on an interactive prompt. 31 new tests: `looksLikeRealText`, `detectLanguage`/`isLanguageAllowed` (including the French-misdetection and Bangla-marks regressions above), `decideFilter`'s full rule cascade plus a stage-level integration test, `runOcrStage` against a fake `OcrProvider` (keep/discard/recognize-failure/lazy-provider-creation-once, all via real temp files to verify actual deletion). `pnpm lint && pnpm test && pnpm build` pass. |
| 6   | LLM layer                     | ✅ Done        | `src/llm/types.ts`: `LLMProvider` (`generateText`/`generateObject` against a Zod schema/`release`) and `Embedder` (`embed`/`release`), the same swappable-interface pattern as `Classifier`/`CollectorDriver`/`OcrProvider`. Built on the Vercel **AI SDK** (`ai` v7, per grill Q14) — Ollama via the community `ollama-ai-provider-v2` (the only Ollama provider compatible with AI SDK v7 + Zod v4; the older `ollama-ai-provider` targets an older SDK major), Groq via the official `@ai-sdk/groq`. **Found a real gap in the Ollama provider**: its typed settings only expose `keepAlive` on the *embedding* model, not the chat model — so `keep_alive: 0` (CLAUDE.md's memory-budget ground rule) is sent via a raw request to Ollama's `/api/generate` endpoint instead (`src/llm/ollamaUnload.ts`, shared by both the chat provider and the embedder) — the documented way to force an unload regardless of which API surface loaded the model. Deliberately does **not** set `keepAlive` on every individual embedding call either, since that would reload the model between every text in a batch; the model stays loaded for the whole stage and `release()` unloads it once at the end, matching the same lazy-create/dispose-once pattern as `ocr.ts`'s tesseract worker. `src/llm/fallbackProvider.ts` implements grill F4's exact policy: `generateText` falls back to Groq once if Ollama is unreachable; `generateObject` retries Ollama up to `maxSchemaRetries` (default 2 — "fails schema validation twice") before falling back; with no fallback configured (Groq disabled by default) it just reproduces the primary's own errors. `src/llm/index.ts`'s `createLLMProvider`/`createEmbedder` wire it to config — Groq only turns on when `config.groq.enabled` **and** `GROQ_API_KEY` is actually set, not just one or the other. 9 new tests, all against fake `LLMProvider`s (no real Ollama/Groq call in the suite, same as the collector/OCR precedent) covering the full retry/fallback decision matrix. `pnpm lint && pnpm test && pnpm build` pass. Not yet exercised live — that needs slice 7 (classification) or later to actually call it, plus the pending `ollama pull embeddinggemma` manual step for the embedder. |
| 7   | Classification               | ✅ Done        | `src/services/classification/`: `Classifier` interface (`classifyBatch`/`release`, same swappable pattern as `LLMProvider`/`OcrProvider`/`CollectorDriver`) built for ADR 0003's bake-off. **Laya** (`layaClassifier.ts`, real `@receptron/laya`/ONNX): multi-label via one `noul` (calibrated P(true)) question per configured category instead of a single `choice`, since categories aren't mutually exclusive (spec's own multi-label example) — plus a `score` question over 4 levels for overall relevance, normalized to 0..1. Loads the English checkpoint by default and the multilingual one lazily, only the first time a non-English post shows up, so an English-only run never downloads the second ~1.7GB bundle. Not unit tested directly (real weights/inference — same rationale as tesseract/Ollama). **gemma4** (`gemmaClassifier.ts`, via `LLMProvider.generateObject`): batched-prompt — groups posts into chunks of 5 and scores every category (+relevance) for the whole chunk in one Ollama call instead of one call per post, per spec's "I care more about minimizing unnecessary inference than real-time speed." Every response is validated against the posts actually sent (right count, indices 0..n-1, no dupes/gaps) before being trusted — CLAUDE.md's "citations are validated, never trusted" rule applied to classification output — with up to 2 local retries before the chunk throws. 14 new tests (`gemmaClassifier.test.ts`) against a fake `LLMProvider` cover the happy path, multi-chunk splitting, one-retry-then-success, and exhausted-retries. New `classification.active: 'laya' \| 'gemma4'` config field (`digest.config.ts` defaults to `laya` — cheap, local, never competes with Ollama for RAM) makes the bake-off a config change, per the ADR. New `postAnalysis` repository (the table already existed from slice 1) upserts one row per post. `src/pipeline/stages/classify.ts`: real stage, same async-work-outside-transaction/commit-per-batch pattern as `ocr.ts`; writes the analysis row for every classified post, then advances only posts at or above `thresholds.relevance` to `classified` (spec's "relevance scoring" step — 60-of-140 in the spec's own example) and drops the rest (`dropReason: 'low_relevance'`) so job extraction/clustering only ever sees posts worth the LLM spend — the analysis row is kept either way, for the UI and for the eventual bake-off eval. **A real bug found and fixed during testing**: the initial batch loop re-queried `listByStatus('filtered', ...)` after a failed batch without excluding the posts that batch just recorded an attempt-failure for, so a broken batch (e.g. Ollama down) spun through all 3 `maxAttempts` and landed every post straight on `failed` within one stage run instead of leaving it `filtered` for a later run to retry — fixed by tracking an in-memory `attemptedThisRun` set and excluding it from each re-query. `src/pipeline/stages/index.test.ts`'s whole-state-machine test now substitutes a fake `Classifier` for the real stage (same DI pattern already used to keep collect/ocr's real providers out of the suite) since the real default (Laya) would otherwise try to download a ~1.7GB model mid test run. Added `@receptron/laya` (pulls in `onnxruntime-node`, whose install script only fetches platform binaries not already bundled — inspected before approving, same as `tesseract.js`'s postinstall; both now allow-listed in `pnpm-workspace.yaml`). The ADR 0003 eval script (scoring both classifiers against labeled posts) is deliberately deferred to slice 11, since it needs real `feedback` rows from the `/posts` labeling UI that slice builds — nothing to score against yet. 9 new stage-level tests (`classify.test.ts`) against a fake `Classifier` cover threshold pass/drop, per-post attempt-failure recording, and single-release-across-batches. `pnpm lint && pnpm test && pnpm build` pass. |
| 8   | Jobs                         | ✅ Done        | Spec's "Job extraction where relevant" step, gated by the classifier's own `job` category score against `thresholds.job` (0.5) — most posts never reach the LLM here at all ("Do not send every raw post to a general-purpose LLM"). `src/services/jobs/`: `extractor.ts` (gemma4 batched-prompt, same shape as `gemmaClassifier.ts` — chunks of 5 posts per Ollama call, every response index-validated against the posts actually sent, up to 2 local retries before throwing); `skillAliases.ts` (`normalizeSkills`, grill G4 — case-insensitive alias dictionary from new `config.jobs.skillAliases`, e.g. `k8s` → `Kubernetes`, unknown skills kept as written). The extraction prompt itself asks a second question per grill G1: `isHiringPost` (someone announcing an open role — recruiter/job-board posts count, job-seeker posts don't), independent of the classifier's own `job` score, since a topically-job-related post isn't necessarily a hiring announcement; a post with several roles gets one `JobOpening` row per role (grill G3). New `jobOpenings` repository (table already existed from slice 1); `list()` added now for slice 9/10's Job Market aggregates. `src/pipeline/stages/extractJobs.ts`: real stage, splits each batch into a skip list (below threshold — committed straight to `enriched` in its own transaction, no LLM touched) and an extract list (its own transaction, only committed if the LLM call succeeds) so one failing LLM call can't roll back the cheap, already-decided skip list; a failed extract list gets per-post `recordAttemptFailure` instead of crashing the run, same `attemptedThisRun` re-query-exclusion fix as slice 7's `classify.ts` bug. Extractor is created lazily (only if a batch actually has a `job`-scored post) and released once at the end, mirroring `ocr.ts`/`classify.ts`'s lazy-create/dispose-once pattern. 8 new tests (`extractor.test.ts`) against a fake `LLMProvider` (role extraction + skill normalization, non-hiring post returns no roles, retry-then-throw on a bad index, release delegation) and 3 (`skillAliases.test.ts`); 5 new stage-level tests (`extractJobs.test.ts`) against a fake `JobExtractor` cover below-threshold skip (no extractor created), hiring-post role storage, non-hiring-post-with-high-job-score (no rows), per-post failure isolation (skip subset still advances when the extract subset's LLM call throws), and single release. `src/pipeline/stages/index.test.ts`'s whole-state-machine test needed no changes — its fake classifier never scores the `job` category above threshold, so the real `extractJobs` stage never actually calls the (real, Ollama-backed) extractor in that test. `pnpm lint && pnpm test && pnpm build` pass. |
| 9   | Cluster + summarize + digest | ✅ Done        | Runs once per pipeline run over the whole current batch of `enriched` posts rather than the usual 25-at-a-time loop — cluster membership is a collective decision, not a per-post one (grill H1/H2: topics recomputed fresh every digest, no persisted cluster identity across runs). **`cluster.ts`**: embeds every `enriched` post (local `embeddinggemma` via the existing `Embedder`), groups by `postAnalysis.primaryCategory`, and clusters each group with a new pure, fully-unit-tested greedy single-pass algorithm (`src/services/clustering/cluster.ts`, `cosineSimilarity` in `similarity.ts`) — an item joins the most similar existing cluster if similarity clears `thresholds.clusterScore` (0.3), else starts a new one; a cluster's `cohesion` (avg member-to-centroid similarity) reuses that same threshold as its viability score, so a cluster is only ever as tight as the bar that built it. Creates the run's `digests` row (`windowStart` = previous digest's `windowEnd`, or the earliest collected post if there isn't one — grill A4) plus `topicClusters`/`topicClusterPosts` (ranked so the `MAX_SOURCES_PER_TOPIC` (3) most-representative members sort first — grill A3) inside one transaction, and advances every clustered post (not just featured ones) to `clustered`. **`summarize.ts`**: per category, ranks clusters by member count and features only the top `MAX_TOPICS_PER_SECTION` (5) (grill A3's "~10-minute read"); each featured cluster gets an LLM-written title + 2-4 cited bullets (`services/summarization/summarizer.ts`, gemma4 via `LLMProvider.generateObject`), each category gets a 2-3 sentence TL;DR (grill H3). **The citation-validation rule CLAUDE.md refers to ("reject and retry, plan §3.1") is implemented here**: every bullet's `sources` must index into the sources actually given to that call or the response is rejected and retried (up to 2 attempts) — grill H5, "every claim... point[s] back to the posts it came from, to protect against hallucination." A cluster or section whose LLM call still fails after retries doesn't fail the run — it's logged and left unfeatured (empty title) / given a deterministic fallback TL;DR, since this whole cycle is one-shot and gets recomputed next time anyway (H2). **`digest.ts`**: finalizes the run's digest — advances every post in any of its clusters (featured or not) to `digested`, and writes `stats` (per-section post counts/TL;DRs plus the spec's Job Market aggregates: companies hiring, roles, most-requested skills, seniority levels, locations — grill G6, current window only — via new pure `services/jobs/aggregate.ts`). New repositories: `digests`, `topicClusters`, `topicClusterPosts` (the tables already existed from slice 1), `digestSections`. 38 new tests: `similarity.test.ts`/`cluster.test.ts` (pure clustering math), `aggregate.test.ts`, `summarizer.test.ts` (fake `LLMProvider`, including the citation-retry-then-throw path), and stage-level `cluster.test.ts`/`summarize.test.ts`/`digest.test.ts` against fake `Embedder`/`Summarizer`. `src/pipeline/stages/index.test.ts`'s whole-state-machine test now also substitutes a fake `Embedder` for `cluster` and a fake `Summarizer` for `summarize`, same DI pattern as `classify`. `pnpm lint && pnpm test && pnpm build` pass. |
| 10  | UI: digest                   | ✅ Done        | shadcn/ui initialized (grill I5) via `pnpm dlx shadcn@latest init` — this shadcn version targets **Base UI** (`@base-ui/react`), not Radix, uses Tailwind v4's CSS-first config, and imports its `cn()` helper from a published `cn` package rather than a local `lib/utils.ts` re-export; components live in `components/ui/` (added `button`, `card`, `badge`, `table`, `separator`, `tabs`, `skeleton`). Dark mode (grill I5) is class-based (`.dark` on `<html>`, matching shadcn's `@custom-variant dark`): a `next/script` `beforeInteractive` snippet applies the stored/OS-preferred theme before hydration so there's no flash, and `components/theme-toggle.tsx` toggles it — deliberately no React state for the icon swap (an `dark:`-variant CSS toggle instead), since setting state from a mount effect trips the `react-hooks/set-state-in-effect` lint rule and a stateful guess would mismatch the server render anyway. New `src/web/context.ts`: one DB connection for the whole `web` process, cached on `globalThis` (the dev-mode-HMR-safe pattern also used for a Prisma singleton) — reads and enqueues `run_commands` only, never writes pipeline data (CLAUDE.md's ground rule); `src/web/digestView.ts` shapes repository rows into page-ready view models (resolves category labels from config, attaches each topic's up-to-3 source posts by rank, filters clusters down to just the ones `summarize` actually gave a title). Pages: `/` redirects to the latest digest (or an empty state); `/digests` lists past digests newest-first (grill H6, browsable historically); `/digests/[id]` renders each section's TL;DR, its featured topics as cards (title, cited bullets, and a numbered source list linking back to the original LinkedIn post — grill H4/H5: author names live in the source list, never the summary text, so every claim stays traceable), and a Job Market section (companies/roles/skills/seniority/locations tables) when the digest has any job openings. **All three routes are forced dynamic** (`export const dynamic = 'force-dynamic'`) — caught in the first `pnpm build`, which silently prerendered `/` and `/digests` as static HTML frozen at build time (shown as `○` in the route table) even though they read live worker-written DB state; `/digests/[id]` was already dynamic from its route param. Manually smoke-tested per CLAUDE.md's UI-changes rule: seeded a throwaway digest (topic with 2 cited bullets, 2 source posts, one job opening) into a scratch `DATA_DIR`, ran `next dev` against it, and curled all three routes — confirmed the TL;DR, topic title, cited bullet text, both linked source authors, and every Job Market table cell actually render; no automated test covers this (no browser in the test suite, consistent with the collector/OCR precedent) so re-verify visually after any further UI changes. `pnpm lint && pnpm test && pnpm build` pass. |
| 11  | UI: operations               | ✅ Done        | **Real settings-table wiring, not just UI**: `settings` rows existed since slice 1 but nothing ever read them back — `loadConfig()` was always called with no overrides, so grill I1/J13's "edits made in the UI are stored in the DB and override the file" was unimplemented. New `src/config/settingsOverrides.ts` (`loadSettingsOverrides`) turns the `settings` table into `loadConfig`'s override argument, one row per top-level `DigestConfig` key; both `worker/main.ts` and `web/context.ts` now two-phase-load (file defaults → open DB → settings overrides → real config) instead of loading config before the DB exists to read overrides from. Documented and deliberate scope limit: a setting saved through the UI takes effect on the **next worker restart**, not mid-run — nothing in the pipeline re-reads config once a run starts. New `app/operations/`: trigger buttons for each `run_commands` type (`full`/`collect`/`process`/`login`) as plain `<form action={...}>`s bound to a Server Action (`repos.runCommands.enqueue` — `web`'s only pipeline-adjacent write, per CLAUDE.md); a recent-runs list linking to `/operations/runs/[id]` for its full event log; and three settings forms (thresholds, active classifier — wired straight to ADR 0003's bake-off, filter lists) that write to `settings` via more Server Actions. **Found the run-events log was nearly empty**: only `collect`'s checkpoint/stop-condition events were ever written to `run_events` — every other stage only logged through pino, invisible to the DB-backed UI grill J11 asked for. Fixed at the one right layer instead of touching all 8 stage files: `pipeline/runner.ts` now writes a `run_events` row alongside its existing logger calls at stage start/finish/failure/awaiting-user/run-succeeded, so `/operations/runs/[id]` shows real per-stage progress for every run. New `app/posts/` (grill I3/D6/E5): a browsable, filterable (status + author/content substring search — new `posts.list()` repository method) list of every collected post including dropped ones with its drop reason and classification, 👍/👎 quick feedback, and a per-post label form (checkboxes over `config.categories` + a 4-level relevance select mirroring Laya's own `score` levels) that writes `feedback` rows of kind `label` — new `feedback` repository (the table existed from slice 1, nothing wrote to it yet). **ADR 0003's eval script now exists** (`scripts/evaluate-classifiers.ts`, `pnpm evaluate:classifiers`): reads every `label` feedback row, runs both Laya and gemma4 against the same labeled posts, and prints precision/recall/F1 per category plus a relevance correlation (new pure, fully-tested `services/classification/evaluate.ts`) — not run in this slice, since real labels don't exist yet (that's the pending manual step after this slice); real-model/real-Ollama scripts never run in the automated suite, same as `pnpm login`. shadcn additions: `input`, `textarea`, `select`, `label`, `checkbox` (select/checkbox added but not wired into a controlled Base UI component yet — the operations/posts forms use plain native `<select>`/`<input type=checkbox>` instead, so pure-HTML-form-plus-Server-Action submission works without any client-side JS). 21 new tests: `settingsOverrides.test.ts`, `posts.test.ts`'s new `list()` cases, `feedback.test.ts`, `evaluate.test.ts` (precision/recall/F1/correlation math, including edge cases: no predictions, zero-variance division). Manually smoke-tested per CLAUDE.md's UI rule: seeded two runs (one finished with real `run_events` rows, one `running`) and two posts (one filtered, one dropped) into a scratch `DATA_DIR`, ran `next dev`, and curled `/operations` (both runs and their statuses render), `/operations/runs/[id]` (both log lines render with their stage tags), `/posts` and `/posts?status=dropped` (the status filter correctly narrows the list). The three trigger-run/settings-save Server Actions were verified by build+typecheck succeeding and their one-line `repos.*` calls being separately covered by repository tests, not a live button click (no browser in this environment) — re-verify by hand before relying on them. `pnpm lint && pnpm test && pnpm build` pass. |
| 12  | Retention + hardening        | ✅ Done        | **Retention** (grill B4/Q7): new `src/pipeline/stages/retention.ts`, wired in as the real final stage (was a no-op stub). A "cycle" is a completed (`succeeded`) run; the purge cutoff is the `finishedAt` of the Nth-most-recent succeeded run (`retention.keepCompletedRuns`, new `runs.listRecentSucceeded()`) — `null` (nothing purged) until that many runs have ever succeeded, so a brand-new install never purges anything. Purges, for every not-yet-purged post collected before the cutoff (new `posts.listPurgeCandidates()`/`purgeContent()`, reusing the existing `runBatchLoop` since this is pure sync DB+fs work): post text, OCR text, and every `post_images` row and file (new `postImages.deleteForPost()`). Keeps everything grill Q7(c) lists — URL, author, `postAnalysis` categories/scores, `jobOpenings` rows, digest/section/cluster summaries — untouched, so old digests' citations still resolve. Debug HTML snapshots (`<dataDir>/snapshots/*.html`, saved on a parse failure since slice 3/4) have no DB row to join against, so they're purged straight from their own filename — `<Date.now()>-<hash>.html` — via a new pure, unit-tested `isSnapshotExpired()`. 8 new tests (`retention.test.ts`) cover: no purge below the cycle threshold, a full purge (post text/OCR/images gone, files deleted, analysis/job rows intact), a post collected after the cutoff left untouched, and expired-vs-fresh snapshot files. **Hardening, both found by re-reading slice 11's own code rather than assigned in advance**: (1) `loadSettingsOverrides` (slice 11) trusted every `settings` row completely — a bad row (malformed manual edit, a future migration, or a forgotten validation gap in some other future writer) would make `digestConfigSchema.parse()` throw and crash **the entire worker or web process on its next startup**, with no obvious cause from the crash message and no way to recover except deleting the row directly from the SQLite file. Fixed by validating each row against its own field's schema (`.partial()` for object sections, via Zod v4's `instanceof z.ZodObject` check) before trusting it — an invalid row is now dropped with a logged warning and that section falls back to the file default, proven with a live repro (`.smoke-seed3.ts`, deleted after): wrote a corrupted `thresholds` row directly, confirmed `getWebContext()` still boots and logs the exact validation error instead of throwing. (2) The three `/operations` settings forms (slice 11) wrote raw, unvalidated `FormData` straight to the `settings` table — the read-side fix already prevented a crash, but a bad submission (e.g. `relevance` left blank → `NaN`) would silently vanish with zero feedback to whoever saved it. Both now run through the same per-field schema validation before writing; an invalid submission is simply not persisted (the old value stays in effect) rather than written and silently dropped later. `ARCHITECTURE.md`'s stage list already named `retention` as real from slice 2's writing; no doc change needed there. 8 new retention tests + 4 new settings-validation tests (2 read-side already in slice 11's file, 2 more here; write-side covered by the same `digestConfigSchema` path, not re-tested separately since it's the identical validator). `pnpm lint && pnpm test && pnpm build` pass — **all 12 slices now done**. |

## Manual steps still pending

- [x] Slice 4: log in once through the "Log in" flow — run `pnpm login`. Done.
- [x] Slice 6: `ollama pull embeddinggemma`. Done (2026-09-25, 621MB).
- [x] ADR 0003 decided (2026-09-25): **gemma4**, via a different path than originally planned —
      instead of hand-labeling ~150 posts, ran `pnpm compare:classifiers` (new script, dumps both
      classifiers' raw output per post to `data/classifier-comparison.json`) and had an external
      model judge the 101-post comparison directly (`bake-off-verdict.md`, repo root). Findings:
      gemma4 clearly more accurate on category, but its own relevance number was badly miscalibrated
      (65/101 posts scored ≥0.8) — fixed by computing relevance deterministically from gemma4's
      category scores instead (new `relevance.ts`, `config.classification.relevanceWeights`) rather
      than trusting an LLM-generated relevance number at all. See `docs/adr/0003-classifier-bake-off.md`
      for the full writeup. `evaluate-classifiers.ts` (the labeled-set precision/recall script) is
      still there and still works if someone labels posts in `/posts` later — nothing consumed
      those labels this time.

## Post-launch fix: scheduler retry-storm (2026-09-25)

Found live: the scheduler fired 10+ `scheduled` runs in under an hour, almost all failing
instantly at `collect` on `browserType.launchPersistentContext: ... profile is already in use`
— a stale Chrome `SingletonLock` (pointing at a PID that no longer existed, left behind by an
unclean exit) that had nothing to do with `checkSchedule` itself, but exposed a real gap in it:
**`stopConditions.maxRunsPerDay` was declared in config from slice 0 but never actually
enforced anywhere.** Its whole purpose (grill B2/Q5: "caps on session length and runs per day"
— an anti-bot-hammering measure, not just a performance one) was exactly what should have
stopped this. Fixed: `checkSchedule` now checks a new `runs.countStartedSince('scheduled',
startOfLocalDay)` against `maxRunsPerDay` before starting another attempt, so a fast-failing
scheduled run stops retrying for the day instead of hammering every 60s indefinitely. 5 new
tests (`scheduler.test.ts`: retry-until-cap, stop-at-cap even while still failing, resets the
next day; `runs.test.ts`: `countStartedSince` counts any outcome but not a never-started run).
Cleared the actual stale lock file by hand (`~/.feed-digester/profiles/main/SingletonLock` etc.
— safe once confirmed the referenced PID wasn't running). `pnpm lint && pnpm test && pnpm build`
pass (272 tests).
