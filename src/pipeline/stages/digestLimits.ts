/**
 * Up to 3 representative source posts shown per insight (grill H4). The old per-section cap of 5
 * topics (grill A3) is superseded by `briefing.maxInsights` — a cross-category ranked cutoff,
 * not a per-category one (spec-second.md §7: "categories should NOT be the primary summarization
 * unit").
 */
export const MAX_SOURCES_PER_TOPIC = 3;
